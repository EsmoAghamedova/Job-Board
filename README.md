# Job-Board
JobBoard — A Flask web app for posting and browsing job listings, with user auth, role-based CRUD permissions, and external API integration. Built as a final project for the Python course.
